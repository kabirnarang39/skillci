models: [claude-sonnet-5]
fail_on: regression

# Required for judge-graded assertions (judge:, and the redteam: plugins
# jailbreak-direct-override/harmful-content-elicitation) — deliberately a
# separate, stronger model than the models under test, since a model can't
# reliably judge itself when it's also the thing that might have drifted.
judge_model: claude-opus-4-8

# Illustrative example rates, not a live Anthropic price feed — see
# internal/config/config.go: skillci never hardcodes or maintains a
# pricing table itself, since Anthropic can reprice without notice.
pricing:
  claude-sonnet-5:
    input_per_million: 3.0
    output_per_million: 15.0

# Any case tagged dimensions: {segment: enterprise} (see evals/dimensions.yaml)
# fails CI on any failure, regardless of the looser fail_on: regression above.
strict_dimensions:
  segment: [enterprise]
